/*
    To the program below, add code to define a struct called personT that includes a field for name (a char array), age
    (an int), and height (a float).
    Then complete the main function to declare a new personT struct variable, initializes all the fields of the variable, and
    prints out the name field of the variable.

*/
#include <stdio.h>
#include <string.h>

// define struct here

int main(void) {

  // implement main


  return 0;
}